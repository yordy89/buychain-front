import { UomToSymbolPipe } from './uom-to-symbol.pipe';

describe('UomToSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new UomToSymbolPipe();

    expect(pipe).toBeTruthy();
  });
});
