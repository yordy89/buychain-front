import { TransportTermsToDescriptionPipe } from './transport-terms-to-description.pipe';

describe('TransportTermsToDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TransportTermsToDescriptionPipe();

    expect(pipe).toBeTruthy();
  });
});
